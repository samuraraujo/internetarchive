Internet Archive Metadata Generator	
===============

Uses the script metadata.rb to generate metadata for a list of MusicBrainz uris. 

	ruby metadata.rb

It reads the file output.txt (SondaSerimi output) and outputs the file metadata.txt.

@Samur Araujo